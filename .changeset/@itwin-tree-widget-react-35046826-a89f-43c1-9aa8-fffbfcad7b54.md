---
"@itwin/tree-widget-react": minor
---

Add a `logger` argument to `TreeWidget.initialize` function to allow supplying a custom logger, with default being `Logger` from `@itwin/core-bentley`. Make sure the logger is used by underlying components.
