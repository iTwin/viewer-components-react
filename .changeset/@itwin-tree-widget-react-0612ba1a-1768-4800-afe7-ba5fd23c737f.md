---
"@itwin/tree-widget-react": major
---

Bumped `@stratakit/*` peer dependencies. Bumped `@itwin/presentation-hierarchies-react` version. `RenameAction` component was renamed to `TreeNodeRenameAction` and now has additional required prop `node`. `FilterAction` was renamed to `TreeNodeFilterAction`. Tree renderers' prop `onFilterClick` was renamed to `filterHierarchyLevel`.
