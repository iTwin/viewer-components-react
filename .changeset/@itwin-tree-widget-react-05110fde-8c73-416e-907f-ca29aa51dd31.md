---
"@itwin/tree-widget-react": major
---

Consume latest `presentation` packages. Derived breaking changes: APIs now operate on `HierarchySearchTree` rather than `HierarchySearchPath`; full class names are now strongly typed with `EC.FullClassName` instead of `string`.
