---
"@itwin/tree-widget-react": patch
---

Fix models tree search to respect custom and unbounded search limits and correctly detect exceeded limits. Fix the models tree to exclude categories containing only elements outside the configured base class.
